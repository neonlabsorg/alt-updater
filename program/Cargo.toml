[package]
name = "alt-updater"
version = "0.1.0"
description = "Address Lookup Table Updater program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = { version = "0.8.4", features = ["std"] }
solana-program = "~2.1.21"

[features]
custom-heap = []
custom-panic = []
